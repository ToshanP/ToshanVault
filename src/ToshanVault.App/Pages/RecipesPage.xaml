<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ToshanVault_App.Pages.RecipesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dg="using:CommunityToolkit.WinUI.UI.Controls">
    <Grid Padding="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Recipes" />
                <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Text="Cookbook of YouTube links and channels. One row per link." />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <Button x:Name="ImportButton" Content="📥 Import xlsx" Click="Import_Click" />
                <Button x:Name="AddButton"    Content="+ Add"          Click="Add_Click"
                        Style="{StaticResource AccentButtonStyle}" />
                <Button x:Name="EditButton"   Content="✏ Edit"         Click="Edit_Click"   IsEnabled="False" />
                <Button x:Name="DeleteButton" Content="🗑 Delete"       Click="Delete_Click" IsEnabled="False" />
            </StackPanel>
        </Grid>

        <AutoSuggestBox x:Name="SearchBox" Grid.Row="1"
                        PlaceholderText="Search title, channel, or URL…"
                        QueryIcon="Find"
                        TextChanged="SearchBox_TextChanged" />

        <InfoBar x:Name="InfoBar" Grid.Row="2" IsClosable="True" IsOpen="False" />

        <dg:DataGrid x:Name="Grid" Grid.Row="3"
                     AutoGenerateColumns="False"
                     SelectionMode="Single"
                     IsReadOnly="True"
                     CanUserSortColumns="True"
                     GridLinesVisibility="Horizontal"
                     HeadersVisibility="Column"
                     SelectionChanged="Grid_SelectionChanged"
                     DoubleTapped="Grid_DoubleTapped">
            <dg:DataGrid.Columns>
                <dg:DataGridTextColumn Header="Title"   Binding="{Binding Title}"      Width="2*" />
                <dg:DataGridTextColumn Header="Channel" Binding="{Binding Author}"     Width="*" />
                <dg:DataGridTextColumn Header="URL"     Binding="{Binding YoutubeUrl}" Width="3*" />
                <dg:DataGridCheckBoxColumn Header="★"   Binding="{Binding IsFavourite}" Width="60" />
            </dg:DataGrid.Columns>
        </dg:DataGrid>
    </Grid>
</Page>
